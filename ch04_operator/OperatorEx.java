package ch04_operator;

import java.util.Scanner;

public class OperatorEx {

	public static void main(String[] args) {
		// 사용자에게 이메일 주소를 입력받으세요
		// email 형식이 아니면 "이메일 형식이 아닙니다. " 맞으면 "사용가능" 출력
		// ex) indexOf 는 특정 문자열을 찾아 인덱스를 반환하는데 없으면 -1을 리턴함
		// 조건: @ 포함 이면 email 형식으로 인정, 없으면 email. 형식이 아님
		
		/*
		 * Scanner scan = new Scanner(System.in); System.out.println("email 주소를 입력하세요");
		 * System.out.println(">>>");
		 * 
		 * String email= scan.nextLine(); System.out.println("email="+ email); int
		 * userInput = email.indexOf("@"); System.out.println("userInput = "+
		 * userInput);
		 * 
		 * String message = (userInput == -1 ? "다시입력" : "사용가능" );
		 * System.out.println(message);
		 */
		
		
		

	}

}
